<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mapsActivity">

    <activity
        android:id="@+id/mapsActivity"
        android:name="com.example.frikiapp.MapsActivity"
        android:label="activity_screen"
        tools:layout="@layout/activity_screen" />
    <fragment
        android:id="@+id/mapFragment"
        android:name="com.example.frikiapp.MapFragment"
        android:label="fragment_map_fragment"
        tools:layout="@layout/fragment_map" />
</navigation>